[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "overload-party-codegen-tools"
version = "0.1.0"
description = "Overload Party サービス系リポ向けコード生成共通ライブラリ"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
