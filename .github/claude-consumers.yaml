# claude-config-sync workflow が同期対象とする消費リポの一覧。
#
# 新リポを追加する場合:
#   1. このファイルに追記
#   2. 当該リポに .claude-common.yaml (layers 宣言) を配置
#   3. CLAUDE_SYNC_TOKEN secret が当該リポへの write 権限を持つことを確認

consumers:
  - repo: kenyamaneko/overload-party-shop
    target_branch: develop
