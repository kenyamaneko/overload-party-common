# Shop constants (SSoT).
#
# Owned by shop service conceptually (per ADR-015), but physically lives in
# common until shop repo owns its own code-gen (ADR-011 / ADR-015 Phase 6).
#
# Per ADR-015 Phase 1, this file is split out of the former data/constants.yaml.
# Consumers: shop service (primary), client (product UI).
#
# Consumed by scripts/generate_constants.py → packages/shop-constants/
#                                            → packages/shop-constants-npm/src/index.ts
# Note: no C# output — battle does not consume shop constants.

product_types:
- faction_set
- cosmetic
- subscription
