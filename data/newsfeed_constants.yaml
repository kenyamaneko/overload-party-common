# Newsfeed constants (SSoT).
#
# Owned by newsfeed service conceptually (per ADR-015), but physically lives
# in common until newsfeed repo owns its own code-gen (ADR-011 / ADR-015 Phase 6).
#
# Per ADR-015 Phase 1, this file is split out of the former data/constants.yaml.
# Consumers: newsfeed service (primary), client (news source filtering/display).
#
# Consumed by scripts/generate_constants.py → packages/newsfeed-constants/
#                                            → packages/gamedata-dotnet/Newsfeed/
#                                            → packages/gamedata-npm/src/newsfeed.ts

cloud_news_sources:
- aws
- google-cloud
- azure
- oci
- other
